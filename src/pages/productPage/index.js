import { memo } from "react";
import './style.scss';
const ProductPage = () => {
    return (
      <div>
        <h1 style={{color:"red"}}>Chưa có sản phẩm</h1>
         </div>
    );


};

export default memo(ProductPage);